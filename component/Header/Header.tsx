import { PageHeader } from "antd";
import React from "react";

const Header = (props: any) => {
	console.log(props);
	return (
		<>
			<PageHeader >adsaf</PageHeader>
		</>
	);
};

export default Header;
